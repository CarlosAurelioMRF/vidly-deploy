@model Vidly.ViewModels.CustomerFormViewModel
@{
    ViewBag.Title = Model.Title;
}

<h2></h2>
@using (@Html.BeginForm("Save", "Customer"))
{
    <legend>@Model.Title</legend>

    <div class="form-group">
        @Html.LabelFor(model => model.Nome, new { @class = "col-lg-2 control-label" })
        <div class="col-lg-10">
            @Html.TextBoxFor(model => model.Nome, new { @class = "form-control", placeholder = Html.DisplayNameFor(model => model.Nome) })
            @Html.ValidationMessageFor(m => m.Nome, null, new { @class = "text-danger" })
        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.MembershipTypeId, new { @class = "col-lg-2 control-label" })
        <div class="col-lg-10">
            @Html.DropDownListFor(model => model.MembershipTypeId, new SelectList(Model.MembershipTypes, "Id", "Name"), "Select Tipo de Membro", new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.MembershipTypeId, null, new { @class = "text-danger" })
        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.Birthdate, new { @class = "col-lg-2 control-label" })
        <div class="col-lg-10">
            @Html.TextBoxFor(model => model.Birthdate, "{0:dd/MM/yyyy}", new { @class = "form-control", placeholder = Html.DisplayNameFor(model => model.Birthdate) })
            @Html.ValidationMessageFor(m => m.Birthdate, null, new { @class = "text-danger" })
        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.IsSubscribedToNewsletter, new { @class = "col-lg-2 control-label" })
        <div class="col-lg-10">
            <div class="checkbox">
                <label>
                    @Html.CheckBoxFor(model => model.IsSubscribedToNewsletter)
                </label>
            </div>
        </div>
    </div>
    @Html.HiddenFor(m => m.Id)
    @Html.AntiForgeryToken()
    <div class="form-group">
        <div class="col-lg-10 col-lg-offset-2">
            @Html.ActionLink("Cancelar", "Index", "Customer", null, new { @class = "btn btn-defaul" })
            <button type="submit" class="btn btn-primary">Salvar</button>
        </div>
    </div>
}

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}
